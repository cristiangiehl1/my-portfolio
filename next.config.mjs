/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'github.com',
      'raw.githubusercontent.com, cdn.devicon.com',
      'cdn.worldvectorlogo',
      'files.svgcdn.io',
    ],
  },
}

export default nextConfig
